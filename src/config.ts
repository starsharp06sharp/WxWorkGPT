const config = {
  port: 3000,
  weork: {
    token: "token",
    encoding_aeskey: "encoding_aeskey",
    receiveid: "",
    robot_name: "GPT 机器人",
  },
  openai: {
    api_key: "api_key",
    max_tokens: 4096,
    timeout_ms: 4500,
  },
};

export default config;
